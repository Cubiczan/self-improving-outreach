# Demo video (YouTube-ready)

`self-improving-outreach-demo.mp4` — silent 1080p H.264 (~73s).

Expanded cut adds three self-improvement beats after the architecture slides:

1. Batch 1 Scout outcomes → LearnEvents  
2. Example ICP weight shifts (demo illustration, not live metrics)  
3. Next draft picks the higher-scoring pattern (`mw-90d`) under the same CHP lock / no auto-send

Source regenerator lives in the marketing pack (`generate_demo.py` + beat PNGs).
