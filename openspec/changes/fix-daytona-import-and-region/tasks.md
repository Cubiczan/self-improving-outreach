# Tasks

- [x] Break `OutreachStore` circular import (lazy `tools` / `chp` package exports, `TYPE_CHECKING` on store protocol)
- [x] Settings + SDK `DaytonaConfig.target` from `DAYTONA_TARGET` / `DAYTONA_REGION`
- [x] OTEL localhost collector unreachable → degrade; local spans remain
- [x] Unit tests: isolated One Daytona import, mocked SDK create+region, OTEL degrade
- [x] README / `.env.example` region + One vs SDK note
- [x] OpenSpec one-integrations delta
