# Tasks

- [x] Queue reclaim helper + CLI `requeue` / `queue reset` + unit tests
- [x] `LEARN_ON_DRAFT` setting; pipeline calls `simulate_outcome` / `apply_learn_event` when opted in; `learn` resolves sample leads
- [x] Confirm `LIVEKIT_FEEDBACK_AUTO` default false; tests with mocks (no LiveKit required)
- [x] `ingest-clickup` + `queue upsert --from-json`; sample ClickUp JSON
- [x] README + `.env.example`; OpenSpec deltas; no paid-ad paths
