# Tasks

- [x] Settings + provider resolution (`RESEARCH_PROVIDER`, `SANDBOX_PROVIDER`, One env knobs)
- [x] `OneCli` subprocess runner (execute / search / knowledge) with mocked tests
- [x] `OneYouComClient` implementing `YouSearcher`; `build_you_client` prefers One
- [x] `OneDaytonaClient` create/start/list/delete; tracer uses it when sandbox provider is One
- [x] Honest `show-config` / `public_settings_view` (no secrets)
- [x] `.env.example` + README (Cubiczan); `.onerc` gitignored
- [x] Unit tests with subprocess mocks; existing mock/direct paths still pass
- [x] OpenSpec: one-integrations + pipeline/swarm deltas
