# Tasks

- [x] `merge_sandbox_create_body` + `OneDaytonaClient.create` merge defaults (buildInfo + optional snapshot)
- [x] Settings / `.env.example` / README for `ONE_DAYTONA_DOCKERFILE` and `ONE_DAYTONA_SNAPSHOT`
- [x] Unit tests for merge and execute `-d` body (mocked CLI)
- [x] OpenSpec one-integrations delta
